<style>
  @media(max-width:480px){
    .quantity{
    
    width:32%;
      float:left;
    }
    .button-box{
    width:32%;
      float:left;
    }
    
    
  }
  
     .quantity{
    
    width:32%;
      float:left;
    }
    .button-box{
    width:32% !important;
      float:left;
    }
</style>
<div class="d-flex align-items-center addtocart-buynow">
                     
             <section id="product-form-wrapper">
  <form id="product-form" action="/cart/add" method="post" data-cart-submit>

    <!-- Price -->
    <h3 id="product-price" style="display:none;" > {{ product.price_min | money }} to {{ product.price_max | money }}</h3>
    <h3 id="product-price">{{product.price | money}} <span><strike> {{product.compare_at_price | money}} </strike></span></h3>

    <!-- Variant select. -->
<!--     <label class="product-name" for="product-select">Choose your</label> -->
    
    <br>
    
    <select class="for-color" id="product-select" name="id">
      {% for variant in product.variants %}
      <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }} - {{ variant.price | money }}</option>
      {% endfor %}
    </select>
    
    
    <!-- Variant swatches. -->
    {% for option in product.options %}
      {% include 'swatch' with option %}
    {% endfor %}
    
        <!-- Custom properties. -->
    {% if product.metafields.properties.engraving %}
    <label for="properties[Engraving]">Engraved message (optional)</label>
    <input type="text" id="properties[Engraving]" name="properties[Engraving]" />
    {% endif %}
    
    <!-- Add to cart button. -->
    <p>
      <input type="submit" id="add-to-cart" class="btn themainbutton btn-primary" name="add" value="Add to Cart" />
    </p>

  </form>
</section>
